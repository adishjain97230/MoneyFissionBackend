services:
  moneyfission:
    build: .
    container_name: moneyfission
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      - ./logging:/app/logging
    restart: unless-stopped
